{
  "name": "weixinmessage",
  "version": "1.0.0",
  "description": "Minimal WeChat ClawBot sender inspired by WeChatCodex",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "send": "node dist/src/cli.js"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^5.9.0"
  }
}
